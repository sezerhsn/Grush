apiVersion: apps/v1
kind: Deployment
metadata:
  name: secrets.example
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secrets.example
  template:
    metadata:
      labels:
        app: secrets.example
    spec:
      containers:
        - name: secrets.example
          image: REPLACE_ME
          ports:
            - containerPort: 3000
